lexik_jwt_authentication:
  secret_key: '%kernel.project_dir%/%env(resolve:JWT_SECRET_KEY)%'
  public_key: '%kernel.project_dir%/%env(resolve:JWT_PUBLIC_KEY)%'
  user_identity_field: email
  token_ttl: 900
